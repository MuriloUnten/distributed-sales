services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq-local
    ports:
      - 5672:5672
      - 15672:15672
    environment:
      - RABBITMQ_DEFAULT_VHOST=assignment1
    volumes:
      - rabbitmq_data:/data

volumes:
  rabbitmq_data:
